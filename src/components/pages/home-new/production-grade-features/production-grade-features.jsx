import Container from 'components/shared/container';

const ProductionGradeFeatures = () => (
    <section className="production-grade-features safe-paddings relative h-screen bg-black-pure">
      <Container className="relative flex h-full flex-col items-center justify-center" size="1344">
        Production Grade Features section
      </Container>
    </section>
  );

export default ProductionGradeFeatures;
