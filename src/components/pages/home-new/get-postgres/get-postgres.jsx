import Container from 'components/shared/container';

const GetPostgres = () => (
    <section className="get-postgres safe-paddings relative h-screen bg-black-pure">
      <Container className="relative flex h-full flex-col items-center justify-center" size="1344">
        Get Postgres section
      </Container>
    </section>
  );

export default GetPostgres;
