---
title: Regions & availability
subtitle: Where your backend runs and how reliably
summary: >-
  Where your Neon backend runs (regions, fixed at creation) and how reliably it stays up (availability, failover, durability) — framed at the whole-backend level. This is the cross-backend concept; region selection/migration reference and per-service durability detail live under Platform and each product.
updatedOn: '2026-08-17T00:00:00.000Z'
---

<Admonition type="info">
**NEW (Concepts) — merge of two planned stubs; TASK SEED.** Combines *Regions* (what regions are, fixed at creation — concept extracted from `introduction/regions`) with *High availability* (durability/failover). Both answer "where/how reliably the backend runs." Open technical questions to resolve before writing: do availability guarantees and region scope cover every service, or just the database? Concept lives here; selection/migration/NAT-IP reference stays in Platform. Redirects folded in from `fundamentals/regions` and `fundamentals/high-availability`. (See `open-questions.md`.)
</Admonition>

# Regions & availability

[Stub: what regions are (fixed at creation) and how availability/failover works, at the whole-backend level. Needs technical exploration of per-service scope; links to Platform for selection/migration reference.]
