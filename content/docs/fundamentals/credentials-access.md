---
title: Credentials & access
subtitle: Who and what can reach your Neon backend
summary: >-
  How access to your Neon backend is scoped: the two-layer access model (organization role plus additive per-project grants) and the credentials that services use (branch-level, per-service tokens and keys). This is the cross-backend concept; reference tables, procedures, and per-service detail live under Platform.
updatedOn: '2026-08-17T00:00:00.000Z'
---

<Admonition type="info">
**NEW (Concepts) — merge of two planned stubs.** Combines the *access model* (two-layer, additive: org role + per-project grants, extracted from `manage/user-permissions`) with *Credentials* (branch-level, per-service tokens — S3 keys for Object Storage, Bearer tokens for AI Gateway). Both answer "who/what can touch the backend." Concept lives here; reference tables + how-tos + per-service management stay in Platform. Redirects folded in from `fundamentals/access-model` and `manage/credentials`.
</Admonition>

# Credentials & access

[Stub: the two-layer access model (org role + per-project grants, additive) and the per-service credential surface (S3 keys, Bearer tokens, branch-level scope). Links to Platform for management procedures and to each product for per-service detail.]
